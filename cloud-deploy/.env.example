# === Public site ===
DOMAIN=157.180.81.235.sslip.io
EMAIL=keithnaidoo@live.com

# === Reverse proxy ===
# Caddy will proxy /api/* to this upstream (Docker service name and port)
API_UPSTREAM=api:8000

# === UI → API (used by UI container) ===
PUBLIC_API_URL=http://api:8000

# === LLM settings ===
LLM_PROVIDER=openai
OPENAI_MODEL=gpt-4o-mini
# OPENAI_API_KEY goes in your real .env, not here

# Optional UI timeout seconds
REQUEST_TIMEOUT=600
